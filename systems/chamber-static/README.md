# P03B CHAMBER STATIC interface instrument

Reserved package for `MZFS.FAM.0008` and `MZFS.GEN.0008`.

Recover the shared chamber-state source into `src/`, retain approved diagnostic renders in `reference/`, and add `reference/preview.jpg`. Document pressure, displacement, residue, anomaly, contour, and log behavior before changing the autonomy mode.
