# FIELD STATION operational instrument

Canonical package for `MZFS.FAM.0005` and the active stateful generator `MZFS.GEN.0002`.

The current source remains at `scripts/generators/field_station_signal_loop.py`. A recovery conversation may migrate the source into `src/`, but it must preserve existing generator ID, state lineage, record continuity, thumbnail output, and event metadata. Approved reference media belongs in `reference/`.
