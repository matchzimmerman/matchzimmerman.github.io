# Modular MAGPIE workstation / painted-interface system

Reserved package for `MZFS.FAM.0009` and `MZFS.GEN.0009`.

Recover digital layout tools, interface modules, and approved animated studies into this package. Source belongs in `src/`; paintings, mockups, and output references belong in `reference/`. Preserve the relationship between physical modules and their digital arrangements.
