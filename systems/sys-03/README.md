# MAGPIE SYS/03 / DOS operational interface

Reserved package for `MZFS.FAM.0001` and `MZFS.GEN.0003`.

When recovering this interface from its original conversation, add executable source to `src/`, approved renders to `reference/`, and a card still at `reference/preview.jpg`. Update `manifest.json` with the conversation title, retained outputs, version, and autonomy mode. Do not scaffold a second SYS/03 family.
