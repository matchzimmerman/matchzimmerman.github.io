# Signal / conformal field instrument

Canonical package for `MZFS.FAM.0007` and active generator `MZFS.GEN.0001`.

The current scheduled SVG source remains at `scripts/generators/signal_field_svg.py`. A recovery conversation may migrate or expand it into `src/` while preserving generator identity and archive continuity. Approved conformal-field stills and loops belong in `reference/`.

## Manual animation studies

### PHASE RELAY 01

Source: `src/phase-relay-01.html`

A browser-native MAGPIE motion study built around an invisible moving influence. The influence is perceived only through field deformation: the line field listens, locks, shears, retains memory traces, and rebuilds over a 19-second deterministic loop. Orange relation traces appear during memory pressure, while the field-audio layer uses low sine components and requires a click/tap gesture to begin.

This study does not replace or modify the scheduled `signal_field_svg.py` archive generator. It remains a manual source study until tested and explicitly promoted.
