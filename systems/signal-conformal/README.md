# Signal / conformal field instrument

Canonical package for `MZFS.FAM.0007` and active generator `MZFS.GEN.0001`.

The current SVG source remains at `scripts/generators/signal_field_svg.py`. A recovery conversation may migrate or expand it into `src/` while preserving generator identity and archive continuity. Approved conformal-field stills and loops belong in `reference/`.
