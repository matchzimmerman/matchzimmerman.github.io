# Archive / HARIL visualization interfaces

Reserved package for `MZFS.FAM.0010` and `MZFS.GEN.0010`.

Recover topographic, threaded-network, braided-helix, archive-weather, and resonance-accumulation systems into `src/`. Retain approved outputs in `reference/`, identify which studies belong to one generator versus related variants, and update the manifest before enabling automation.
