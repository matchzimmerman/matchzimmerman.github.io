# 32-bit CRT display protocol

Reserved package for `MZFS.FAM.0006` and `MZFS.GEN.0007`.

Recover the reusable display treatment into `src/`, including 4:3 geometry, rounded-glass curvature, bitmap text, and the rolling exposure band. Retain approved examples in `reference/` and update the manifest before enabling any autonomous run.
