# Thermal behavioral cartography

Reserved package for `MZFS.FAM.0004` and `MZFS.GEN.0006`.

Recover the sensor and motion system into `src/`, retain approved outputs in `reference/`, and add `reference/preview.jpg`. Update the manifest with the original conversation, input assumptions, audio behavior, version, and autonomy mode. Do not create a duplicate behavioral-cartography family.
