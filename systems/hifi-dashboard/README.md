# Hi-fi dashboard

Reserved package for `MZFS.FAM.0003` and `MZFS.GEN.0005`.

Recover the continuous dashboard source into `src/`, retain approved renders in `reference/`, and add `reference/preview.jpg`. Update the manifest with provenance, runtime version, and autonomy mode. Do not create a second hi-fi dashboard family.
