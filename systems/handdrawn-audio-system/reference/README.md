# Reference outputs

- `preview.jpg` is the public system card image.
- `iterations/` contains the fifteen web-optimized MP4 stages recovered from the conversation.
- `iterations.json` records titles and development notes.

These are approved conversation references, not autonomous archive executions.
